## 1. Full-Length Dash Stroke Import

- [x] 1.1 Implement `Full-length SVG stroke dash arrays import as solid strokes` in the module Figma adapter before `createNodeFromSvg`.
- [x] 1.2 Implement `Full-length SVG stroke dash arrays import as solid strokes` in the classic Figma runtime before `createNodeFromSvg`.

## 2. Verification

- [x] 2.1 Add tests proving the checked icon full-length `stroke-dasharray` is removed.
- [x] 2.2 Add tests proving a real short dashed SVG stroke keeps its `stroke-dasharray`.
- [x] 2.3 Run build, focused tests, full tests, and Spectra validation.
